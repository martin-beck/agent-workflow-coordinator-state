# AR-0041 plan: runtime dispatch admission

1. Add a fixed-entrypoint resolver/dispatcher around `DispatchAdmission` with descriptor-bound path checks.
2. Reject caller-supplied script/path overrides and all ancestor/entrypoint aliases.
3. Add subprocess tests for selected release, selector replacement, runtime replacement, and argument-only variation.
4. Keep signature/trust-policy verification delegated to the existing admission verifier; do not weaken authenticity requirements.
5. Run full quality/formal/hosted/post-merge gates and reconcile the AR.

