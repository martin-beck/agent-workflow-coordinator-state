# AR-0043: Bind process-death barrier evidence to the formal contract

1. Inventory every durable barrier transition and effect boundary in the current
   SQLite/control-store implementation.
2. Map each existing independent-process test to the corresponding model action,
   recording exact test selectors and expected durable state.
3. Add only missing bounded tests for SIGKILL, close/fsync uncertainty, WAL/SHM
   replacement, and recovery under stale or reused fencing identity.
4. Extend the machine-readable contract and evidence map with exact hashes and
   explicit exclusions for operating-system durability semantics that are not
   modeled.
5. Run the bounded TLC tier and full quality gates. Do not claim mathematical
   Python-to-TLA refinement and do not enable upgrade mutation.
