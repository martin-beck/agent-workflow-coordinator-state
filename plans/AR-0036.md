# AR-0036 plan: Git backup-only executor parity

1. Reuse only Git-owned `create_backup_bound`, `verify_backup_artifact`, and lifecycle-session primitives; do not call SQLite code.
2. Validate the exact generated operation envelope and bind operation/session/barrier/fence identity before effect.
3. Hold the existing lock/admission scope through backup and post-effect reread; publish only a durable terminal outcome accepted by the AR-0032/0033 chain.
4. Add hostile subprocess tests for repository replacement, clean-tree drift, backup path swaps, verifier failure, and process death.
5. Keep all other phases and generic `execute` disabled until a separate AR proves their contracts.
