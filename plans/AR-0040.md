# AR-0040 plan: bind SQLite evidence to the refinement contract

1. Extend the v10 refinement contract and map with route-level implementation references for task, observation, command-result, migration, retirement, and baseline provisioning.
2. Add a deterministic evidence generator/check that records exact source/test digests and rejects stale or missing references.
3. Preserve explicit bounded-model and non-refinement claims; no upgrade apply/rollback enablement.
4. Add regression tests for stale evidence, omitted routes, and mismatched implementation heads.
5. Run TLC/formal, full quality, hosted, post-merge, and state doctor gates.

