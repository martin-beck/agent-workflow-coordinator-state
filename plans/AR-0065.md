# AR-0065 plan: Role registry schema and checker

1. Define role.schema.json and role-registry.schema.json with versioned identifiers.
2. Encode the capability matrix, forbidden actions, tool/worktree policy, and AWQ profile refs.
3. Add positive and hostile fixtures and an autonomous checker.
4. Register the schemas in the public-contract inventory and run AWQ gates.
