# AR-0038 plan: bind public SQLite mutation routes

1. Inventory all `handoffctl` SQLite writes and classify read-only, migration, and mutating paths.
2. Add one adapter-owned factory that validates the provisioned authority/control marker, binds `SQLiteBarrierSessionStore` and `MutationFence`, and returns `SQLiteBackend` only with the concrete `LockDomainScope`.
3. Route normal task mutations, command-result appends, observation reconciliation, migration, and retirement through that factory.
4. Preserve legacy installations by keeping reads available and rejecting only mutation/upgrade when provisioning evidence is absent.
5. Add route-level and independent-process hostile tests for barrier states, control-store/WAL/SHM replacement, lock timeout, and process death.
6. Run full quality/formal gates, merge only at exact reviewed head, require post-merge Verify, and reconcile this AR with the exact evidence.

