# AR-0033 plan: integrate the session chain with the durable contract

1. Add a narrow adapter in `tools/durable_session_contract.py` that accepts only canonical AR-0032 records and returns validated immutable records for diagnostics/recovery.
2. Keep all existing mutation/dispatch flags false and reject uncertain records unless they are explicitly terminal `ambiguous` records; never infer continuation from a legacy flat journal.
3. Add contract-level tests that exercise the adapter and prove no existing admission or executor path calls it to authorize work.
4. Run the focused recovery suite, repository quality checks, fast TLC, hosted gates, and post-merge exact-head verification.
5. If an implementation gap requires a new schema or authority mutation, stop and create another child AR instead of broadening this slice.
