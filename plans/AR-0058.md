# AR-0058 plan: authenticated selector and runtime execution boundary

1. Bind selector and runtime descriptors to the AR-0056 proof obligations and
   AR-0057 barrier session.
2. Finish no-follow retained-descriptor reads, canonical bytes, and identity
   revalidation across validation and execution.
3. Implement atomic selector publication with fsync ambiguity classification;
   never overwrite the stable bootstrap.
4. Exercise the full hostile selector/runtime and crash-recovery cross-product.
5. Keep dispatch and replacement disabled until independent review and formal
   evidence agree.
