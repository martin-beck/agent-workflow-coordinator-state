# AR-0069 plan: Roles release and downstream pinning

1. Define the canonical release manifest and SPDX graph.
2. Materialize bounded distribution archives.
3. Publish the downstream lock manifest and verify byte-identical outputs.
