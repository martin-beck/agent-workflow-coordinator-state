# AR-0035 plan: bounded backup-only executor seam

1. Define one canonical generated backup opcode and map it to the existing typed Git/SQLite backup primitives without sharing authority assumptions.
2. Require a held barrier, immutable session-chain continuation, exact authority/control/journal identities, and the ordered lock domain before effect.
3. Capture a durable pre-effect record, perform only the backup, reread identities and verify the backup, then append a durable terminal outcome. Any uncertain write/fsync/process death enters ambiguous safe mode.
4. Add hostile and process-death tests for every boundary and prove no selector/runtime or dispatch state changes.
5. Extend the bounded formal model/refinement only for this opcode. Keep all other executor phases rejected.
6. Run focused and full gates, merge only with exact-head evidence, then update AR-0031 and create the next child only if a new independent seam is found.
