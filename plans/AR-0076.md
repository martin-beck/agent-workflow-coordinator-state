# AR-0076 plan: Durable work-session snapshot record

1. Define the session record schema (context digest, step state, artifact refs, next action).
2. Append session records on update/run with bounded retention.
3. Add replay-from-snapshot tests and privacy/size findings.
