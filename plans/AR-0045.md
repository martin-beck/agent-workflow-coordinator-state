# AR-0045: Repair backup executor coverage gate regression

1. Add tests for incomplete contexts, malformed bindings, backend exceptions,
   already-classified errors, invalid result shapes, and all postcondition failures.
2. Run module coverage and the full repository suite.
3. Publish the corrective PR from exact merged main, then require hosted and
   post-merge gates before releasing the AR.
