# AR-0017 plan: TLC resource-bound reliability

1. Define a capacity-safe resource profile for required same-repository formal runs without changing models, invariants, or attestation semantics.
2. Raise TLC heap and matching systemd cgroup memory/swap bounds only after host-capacity preflight; fail closed when headroom is insufficient.
3. Add focused runner/evidence tests covering the bounds, admission behavior, and exact model-set preservation.
4. Run independent review, an exact-head publication gate, and the weekly/full exhaustive gate; record terminal evidence.
5. Do not claim implementation refinement or reduce state-space coverage; split model slicing into a separate AR if capacity remains insufficient.
