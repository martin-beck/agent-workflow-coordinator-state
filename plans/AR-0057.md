# AR-0057 plan: production durable barrier and SQLite fencing

1. Derive the trusted binding/session factory from the AR-0056 contract.
2. Implement strict CAS transitions and ordered lock admission with durable
   identity/fencing-token rereads.
3. Route every SQLite write path through the binding and stable rejection
   errors; preserve compatibility for legacy read-only installations.
4. Add hostile replacement, stale revision, owner forgery, WAL/SHM swap,
   timeout, process-death, and recovery tests in independent processes.
5. Publish only bounded evidence; do not enable apply or rollback.
