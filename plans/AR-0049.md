# AR-0049 plan: selector publication admission boundary

1. Audit the exact post-AR-0048 selector, manifest, durable-revision, fencing,
   and barrier contracts on product main.
2. Specify the authenticated compare-and-swap admission record and its
   correspondence to the bounded formal model.
3. Implement only a read-only admission/evidence seam; do not write the
   selector or dispatch runtime replacement.
4. Add hostile tests for stale revisions, digest drift, replacement races,
   uncertain reads, process death, forged contexts, and fencing mismatch.
5. Run the complete local suite, branch coverage at or above 95%, Ruff, mypy,
   formal evidence checks, and exact-head hosted gates.
6. Merge only with green PR checks and post-merge Formal/Verify success; then
   reconcile and run `handoffctl doctor --live` before closing the AR.
