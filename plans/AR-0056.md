# AR-0056 plan: complete formal refinement proof gate

1. Freeze the exact model/config/tool/source revisions and generate a
   machine-readable obligation matrix.
2. Add correspondence rows for every already-merged bounded seam and explicit
   non-claims for all mutation transitions.
3. Add trace-preserving tests for ordering, fencing, ambiguity, selector
   visibility, journal durability, crash recovery, and coordinator availability.
4. Run the fast merge model on every change and the exhaustive model weekly;
   classify timeout, cancellation, and orphaned checks as unknown.
5. Have an independent reviewer inspect the mapping and require exact-head
   hosted checks plus post-merge Formal/Verify before changing the gate.
