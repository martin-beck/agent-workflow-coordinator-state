# AR-0059 plan: bounded executable mutation and rollback slices

1. Start from the exact AR-0056/0057/0058 contracts and keep unsupported
   operations rejection-only.
2. Implement backup and durable outcome recording for Git and SQLite as
   separate bounded slices.
3. Inject failures at every durability and process boundary and prove old/new
   functional continuity plus evidence-preserving ambiguous recovery.
4. Promote selector publication, runtime replacement, commit, apply, and
   rollback only after a new independent review and formal mapping for each.
5. Reconcile state after every merge and post-merge check.
