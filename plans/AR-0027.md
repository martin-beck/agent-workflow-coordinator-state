# AR-0027: Batched TUI packet and navigation binding

Extend event binding for list/tree sessions, preserving item identity and
document anchors through navigation and partial responses. Keep rendering in
AWG and validation in AWQ; Coordinator stores typed references and enforces
transition safety.

Acceptance must cover independent, coupled, and partially answered packets.
