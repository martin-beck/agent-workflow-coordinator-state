# AR-0034 plan: SQLite connection lifecycle repair

1. Trace every connection creation reachable from barrier reads and control-store observations, including injected failure paths.
2. Replace implicit ownership with explicit context managers or `try/finally` close blocks; preserve transaction and lock ordering.
3. Add focused tests that run the relevant hostile matrix with `ResourceWarning` promoted to errors.
4. Run the full AR-0012 subset and repository gates; create another child AR if the trace reveals a separate ownership defect.
