# AR-0050 plan: runtime replacement admission boundary

1. Audit the exact post-AR-0049 staged-runtime, manifest, selector, backend,
   fencing, and barrier contracts on product main.
2. Specify the immutable runtime replacement admission record and its bounded
   formal correspondence.
3. Implement only read-only staged-runtime admission; do not replace the
   active runtime or publish a selector.
4. Add hostile tests for missing/foreign/symlinked/replaced artifacts,
   manifest and selector digest drift, process death, fencing mismatch, and
   uncertain reads.
5. Run the complete suite, branch coverage at or above 95%, Ruff, mypy,
   formal evidence checks, and exact-head hosted gates.
6. Merge only after green PR checks and post-merge Formal/Verify success; then
   reconcile and run `handoffctl doctor --live` before closing the AR.
