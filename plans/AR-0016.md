# AR-0016 plan: release validation dispatch

1. Define the exact release-sensitive path set and event/dispatch contract.
2. Add a negative regression proving release candidates cannot silently skip full Verify.
3. Validate ordinary pull-request fast paths remain bounded and unchanged.
4. Run workflow/configuration lint and local contract tests; record exact CI evidence.
5. Obtain independent review before any future release publication.
