# AR-0021: Reconcile coordinator issue #14 with the released AWQ adoption

## Scope

Reconcile GitHub issue #14 against the already completed coordinator AWQ
adoption. Do not introduce a second quality integration or downgrade the
tracked release. The issue requests the nine profiles and generated policy,
lock, and launcher artifacts; AR-0001 already delivered these through PR #20
using the newer released AWQ v0.32.0 baseline.

## Acceptance

- Verify the exact merged PR, current product artifacts, requested profiles,
  native-gate retention, and green exact-head checks.
- Record that v0.32.0 supersedes the issue's v0.24.0 baseline and that no
  duplicate implementation AR is required.
- Add a privacy-safe issue comment and close issue #14 as superseded by the
  verified AR-0001 result.

## Non-goals

Do not rewrite the lock to v0.24.0, remove native gates, modify formal
evidence contracts, or change the coordinator runtime.
