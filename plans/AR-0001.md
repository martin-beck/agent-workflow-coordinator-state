# AR-0001: Integrate AWQ v0.32.0 into the coordinator

## Scope

Add the latest released Agent Workflow Quality policy and CI integration to
the coordinator product without replacing native coordinator gates.

## Acceptance

- Product tracks `quality/awq.json`, `quality/awq.lock.json`, and `tools/awq`.
- The AWQ lock pins v0.32.0 and its full reviewed commit.
- Matching profiles are enabled; terminology is handled only with a reviewed
  coordinator vocabulary registry.
- CI executes AWQ from an immutable source commit and keeps the existing native
  verification job required.
- AWQ PR checks, native tests, formal models, source-header checks, vendor
  verification, privacy review, and DCO checks pass on the exact candidate.
- The state repository remains Git-backed and its vendor manifest is unchanged.

## Next action

Implement the product policy and pinned CI job in the named product worktree,
then run the complete acceptance gates and record exact results.

