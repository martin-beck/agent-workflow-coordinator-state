# AR-0048 plan: selector/runtime readiness validation

1. Audit the retained `DispatchAdmission` and selector/manifest revalidation
   contracts at exact product main; define the smallest trusted consumer seam.
2. Specify the generated validate operation and immutable identity binding;
   reject path-only, forged, closed, stale, or ambiguous admission values.
3. Implement only read-only readiness evidence in `UpgradeEngine`'s validate
   phase; preserve fail-closed commit/apply/rollback and selector publication.
4. Add hostile replacement, digest drift, close uncertainty, and admission
   identity tests; map only implemented model actions without refinement claims.
5. Run focused and complete tests, Ruff, mypy, and >=95% branch coverage; publish
   an exact-head PR with DCO and hosted gates.
6. Merge only after post-merge Formal/Verify success, then reconcile and run
   `handoffctl doctor --live`.
